// Centralized feature toggles for content that is not ready for publication.
export const featureFlags = {
  showHomeGallery: false,
  showOpenRoleResourceCards: false,
};

export default featureFlags;